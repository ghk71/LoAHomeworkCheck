# Feature Spec

## 1. 공통

- 모든 페이지는 같은 디자인 톤을 유지한다.
- 화면 하단에 JavaScript 코드가 노출되면 안 된다.
- `</html>` 뒤에는 어떤 코드도 있으면 안 된다.
- CSS 변수 `var(--...)`를 쓰면 반드시 `:root`에 정의한다.
- GitHub Pages에서 별도 빌드 없이 동작해야 한다.

## 2. index.html

### 계정

- 계정 추가/수정/삭제
- 계정 숨김
- 계정 순서 드래그
- 부계정 설정
- 부계정은 본계정 아래 하위 탭으로 표시

### 캐릭터

- 캐릭터 추가/수정/삭제
- 캐릭터 순서 드래그
- 캐릭터 아이콘 선택
- 아이콘은 `icon_url`에 저장
- 캐릭터별로 레이드 숙제 / 재화 현황 / 커스텀 노트 표시 여부 설정
- 비활성화한 캐릭터 섹션은 데이터 삭제 없이 숨김 처리하며, 캐릭터 수정에서 다시 활성화 가능

### 원정대 숙제

- 추가/수정/삭제
- 아이콘 선택/제거
- 체크 상태 저장
- 다른 계정으로 복제
- 개별 숙제를 일시중지할 수 있으며, 중지 중에는 표시/완료 집계/초기화/휴식 게이지 처리에서 제외
- 일시중지 관리는 일반 비활성 숙제 보기와 분리하고, 별도 관리 창에서 다시 활성화

### 캐릭터 숙제

- 추가/수정/삭제
- 아이콘 선택/제거
- 하위 숙제 관리
- 일일/주간/월간(매월 첫째 주 수요일 06:00 KST) 초기화
- 월간 숙제는 별도 칸이 아니라 주간/월간 숙제 칸에 표시
- 휴식 게이지 설정/현재값 표시
- 휴식 게이지 사용 시 강조 포인트 미만 숙제는 기본 숨김, 활성화 보이기에서 표시
- 체크 상태 저장
- 개별 숙제를 일시중지할 수 있으며, 상위 숙제 중지 시 하위 숙제도 함께 숨기고 처리에서 제외
- 일시중지 해제 시 중지 기간의 누락 초기화나 휴식 게이지를 소급 적용하지 않음
- 캐릭터 업데이트 버튼은 캐릭터/주간·월간/계정/일회용 탭에서 항상 표시
- 일회용 숙제는 캐릭터별 카드로 구분하고, 캐릭터를 해당 화면에서 숨김/다시 표시 가능

### 레이드 숙제

- 레이드명 + 난이도 표시
- 입장 레벨 표시
- 유통골드/귀속골드/더보기 골드 분리 표시
- 각 골드 항목에 hover 효과
- 각 골드 항목은 수령 여부를 별도로 토글
- 레이드 숙제 항목에서는 수정 버튼을 막음
- 실제 파티 배치 또는 임시 파티 배치가 있을 때만 파티연동 배지 표시
- 파티연동 팝업은 해당 캐릭터가 포함된 파티만 표시
- 파티 이동 버튼 제공

### 재화

- 재화 추가/수정/삭제
- 금액 직접 입력
- ± 버튼으로 금액 조정
- 아이콘 선택/제거
- 재화현황에는 최종 수정 날짜를 별도로 표시하지 않음

## 3. core.html

- 1700 이상 캐릭터 표시
- 기본 보기
- 번호별 묶음 보기
- 사용자 조합 보기
- 상단 보기 탭은 raid.html 탭 스타일과 유사하게 구성
- 마우스 휠 스크롤 가능해야 함
- 코어 등급 클릭 변경
- 필수 별표 토글
- 혼돈해1/혼돈달1/혼돈별1 텍스트 클릭 편집
- 변경된 혼돈 이름은 Supabase DB에 저장
- 사용자 조합 카드 크기는 과도하게 크지 않게 유지
- 등급 배지 hover 효과 제공

## 4. raid.html

- 기본 탭은 주간 일정
- 파티 구성 탭
- 레이드 프리셋 추가/수정/삭제
- 레이드 그룹 아이콘과 색상은 DB(`raid_group_settings`)에 저장
- 난이도 추가/수정/삭제
- 파티 추가/수정/삭제
- 파티 멤버 배치
- 입장레벨 필터
- 일정 추가/수정/삭제
- 주간 일정 카드에서 일정 즉시 삭제, 멤버 임시 제거, 빈 슬롯 임시 추가 가능
- 8인 파티는 파티 구성과 주간 일정에서 1파티 4명 / 2파티 4명으로 구분
- 주간 일정 완료/완료 해제
- 임시 파티 추가/삭제
- 원본 초기화
- 주차 이동
- 공지사항 표시
- 공지 아래 주차별 댓글 표시
- 공지 댓글 영역 접기/펼치기
- 공지 댓글은 공유 링크 뷰어에서도 작성 가능
- 공지 댓글은 현재/미래 주차별로 분리 저장
- 편집모드와 공유링크 뷰어모드 모두 공지가 잘 보여야 함
- 공유링크 뷰어모드에서 레이드 현황 탭 표시
- 공유링크 레이드 현황에는 캐릭터 필터 표시
- 공유링크 레이드 현황 캐릭터 카드에 골드 정보와 레이드순/요일순 정렬 토글 표시
- 공유링크에서는 완료 체크 불가, 상태 표시만 가능

## 5. overview.html

- 계정/부계정 그룹화
- 캐릭터별 레이드 완료 현황 표시
- 파티연동 팝업은 해당 캐릭터가 포함된 파티만 표시
- 완료 상태는 index.html 및 raid.html 완료 처리와 연동
- 캐릭터 필터는 페이지 진입 시 항상 접힘
- 파티 미연동 및 임시해제 레이드 행은 카드 안에서 강하게 강조

## 6. parties.html

- 레이드명 옆에 난이도 표시
- 난이도 색상 유지
- 파티 정보 하단에서 난이도 중복 표시 제거
- 파티 멤버는 슬롯 순서대로 표시
- 빈 슬롯 표시

## 7. party_generation.html

- 캐릭터 필터는 페이지 진입 시 항상 접힘
- 레이드/난이도/파티 순서는 `raid.html` DB 순서를 따름
- 모든 난이도가 숨김 처리된 레이드도 상단 레이드 탭은 유지하고 복구 동선을 제공
- 작업안, 숨김 난이도, 후보 선택 상태는 `party_generation_drafts`에 저장
- 적용 전 검증 및 DB 원본 복구/전체 초기화 기능 제공
