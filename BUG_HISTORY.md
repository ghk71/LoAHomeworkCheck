# Bug History

이 파일은 Codex 또는 사람이 수정한 버그 이력을 누적 기록하는 파일이다.

## 작성 규칙

새 버그를 고칠 때마다 아래 형식으로 추가한다.

```md
## YYYY-MM-DD - 짧은 제목

### 증상
- 무엇이 안 됐는지

### 원인
- 코드/스키마/디자인/상태관리 중 어디가 문제였는지

### 수정
- 어떤 파일을 어떻게 고쳤는지

### 검증
- 어떤 테스트를 했는지
```

---

## 2026-04-28 - index.html 하단 JS 노출

### 증상

`index.html` 화면 하단에 `async function showPartyLinkPopup...` 코드가 그대로 노출됨.

### 원인

JavaScript 함수가 `</html>` 뒤에 붙어 있었다.

### 수정

- `showPartyLinkPopup()`을 `<script>` 내부로 이동
- `</html>` 뒤 코드 제거
- 함수에 예외 처리 추가

### 검증

- `</html>` 뒤 코드 없음
- `node --check` 통과

---

## 2026-04-28 - core.html CSS 깨짐

### 증상

사용자 조합 카드, 버튼, 상단 탭 CSS가 깨짐.

### 원인

템플릿에서 사용하는 클래스가 CSS에 정의되지 않았음.

### 수정

- `combo-add-btn`
- `combo-entry`
- `combo-grade-row`
- `ccard-empty`
- 기타 사용자 조합 카드 스타일 보강

### 검증

- CSS 변수 누락 검사
- 브라우저 수동 확인 필요

---

## 2026-04-28 - raid.html 파티 선택 오류

### 증상

파티 구성 탭에서 특정 파티 클릭 시:

```txt
ReferenceError: gPresets is not defined
```

### 원인

`renderPartyDetail()` 내부에서 스코프 밖 변수 `gPresets`를 참조함.

### 수정

해당 파티의 preset 정보를 직접 조회하도록 변경.

### 검증

- 파티 선택 시 콘솔 에러 없어야 함
- 여러 레이드/난이도/파티 모두 클릭 테스트 필요

---

## 2026-04-28 - 레이드 골드 표시 불일치

### 증상

유통골드/귀속골드/더보기 골드가 분리되지 않거나 귀속골드 토글이 없음.

### 원인

`receive_bound` 컬럼 및 UI 토글 로직이 누락됨.

### 수정

- `raid_tasks.receive_bound` 추가
- index UI에서 유통/귀속/더보기 분리 표시
- 각 항목 토글 분리

### 검증

- 세 골드 각각 토글
- 새로고침 후 상태 유지

---

## 2026-04-28 - 일일숙제 수정 버튼 오류

### 증상

일일숙제 수정 버튼 클릭 시:

```txt
ReferenceError: setTaskIcon is not defined
```

### 원인

아이콘 선택 기능 추가 중 함수명이 누락되었거나 연결되지 않음.

### 수정

- `setTaskIcon()` 구현 또는 기존 아이콘 setter와 연결
- 일일숙제/원정대숙제 아이콘 선택/제거 정상화

### 검증

- 일일숙제 수정 모달 열기
- 아이콘 선택
- 저장 후 새로고침

---

## 2026-04-28 - 공유링크 레이드 현황 필터 누락

### 증상

공유링크 뷰어 모드의 레이드 현황 탭에 캐릭터 필터가 없음.

### 수정

- 뷰어용 overview 렌더링에 캐릭터 필터 추가
- 체크박스는 상태 표시만 하고 수정 불가 처리

### 검증

- 공유 링크 시크릿 창 접속
- 필터 동작 확인
- 완료 상태 클릭으로 DB 수정되지 않는지 확인

---

## 2026-04-28 - parties.html 난이도 위치

### 증상

난이도가 레이드명 아래에 표시됨.

### 수정

- 난이도를 레이드명 옆으로 이동
- 하단 중복 표시 제거
- 난이도 색상 클래스 유지

### 검증

- 나이트메어/하드/노말 색상 확인
- 중복 난이도 표시 없는지 확인

---

## 2026-04-29 - 파티 슬롯 제거 시 레이드 숙제 삭제 문제

### 증상
- `raid.html` 파티 구성에서 캐릭터를 다른 슬롯/파티에 잘못 배치한 뒤 제거하면, 해당 캐릭터의 `index.html` 레이드 숙제가 삭제되거나 연동이 불필요하게 해제될 수 있음.

### 원인
- 파티 슬롯 제거 함수가 `raid_party_members`만 지우지 않고 `raid_tasks` 삭제/연동 해제까지 함께 수행함.

### 수정
- `raid.html`의 일반 파티 슬롯 제거는 파티 멤버만 제거하도록 변경.
- 같은 레이드 프리셋의 다른 슬롯에 같은 캐릭터를 다시 배치하면 기존 중복 슬롯을 먼저 정리하도록 변경.

### 검증
- `node tools/check-project.js` 통과.
- `git diff --check` 통과.

---

## 2026-04-29 - 파티연동 없는 레이드에 연동 배지 표시

### 증상
- `preset_id`만 있는 레이드 숙제도 실제 파티 배치가 없는데 `파티연동` 배지가 표시됨.

### 원인
- `index.html`, `overview.html`, 공유 링크 레이드 현황이 실제 파티 멤버 여부가 아니라 `raid_tasks.preset_id`만 보고 배지를 표시함.

### 수정
- 실제 `raid_party_members` 배치 또는 이번 주 임시 파티 추가가 있을 때만 파티연동 배지를 표시하도록 변경.
- 공유 링크 레이드 현황의 요일 표시도 실제 파티 배치가 있는 경우에만 표시하도록 변경.

### 검증
- `node tools/check-project.js` 통과.
- `</html>` 뒤 코드 없음 확인.

## 2026-08-13 - 주차 간 임시 레이드 연결 및 복제 트리 무결성

- 과거 주차 임시 기록을 보존하는 동안 `raid_tasks.preset_id` 복원이 지연될 수 있던 경로를 수정했습니다. 과거 JSON은 `_restored` 표식과 함께 3주 유지하고 전역 연결만 새 주에 즉시 복원합니다.
- 같은 숙제가 여러 주차 임시 기록에서 참조될 때 다른 주의 정리 작업이 삭제/연결 변경을 하지 않도록 활성 참조 수를 검사합니다.
- 원래 파티에서 임시해제한 캐릭터를 다른 파티에 임시 추가했다가 제거할 때 원래 프리셋 연결이 끊길 수 있던 전이 규칙을 수정했습니다.
- 숙제 복제가 직계 하위만 복제하던 문제를 수정해 임의 깊이 자손과 부모 관계를 모두 보존하고, 같은 복제 그룹의 대상 중복 생성을 차단합니다.
## 2026-08-13 - 하위 숙제 진행도·일정 즉시 삭제·숨김 난이도 복구

- 하위 숙제 진행도를 중복 제거하는 과정에서 부모 숙제 행의 진행도까지 사라진 문제를 수정했습니다. 부모 행 우측에 `하위 숙제 완료/전체`를 한 번만 표시합니다.
- `raid.html`의 일정 삭제 확인창을 제거하고, 중복 클릭만 방지하면서 즉시 원자적 삭제 RPC를 호출하도록 변경했습니다.
- `party_generation.html`에서 난이도가 일부만 숨겨진 경우에도 선택 레이드 상단에서 숨긴 난이도를 개별 또는 전체 복구할 수 있도록 수정했습니다.

## 2026-08-18 - 하위 숙제 완료 후 상위 숙제가 간헐적으로 미완료

### 증상
- 마지막 하위 숙제들을 빠르게 완료하면 모든 하위 숙제가 완료됐는데도 상위 숙제가 가끔 미완료로 남았습니다.

### 원인
- 각 하위 숙제 요청이 클릭 당시 계산한 상위 완료값을 함께 저장했습니다. 요청 응답 순서가 바뀌면 먼저 계산된 `미완료` 값이 나중의 `완료` 값을 다시 덮을 수 있었습니다.

### 수정
- 화면은 클릭 직후, 저장 성공, 실패 롤백마다 현재 하위 상태로 모든 조상 완료 상태를 다시 계산합니다.
- DB 함수는 변경 행 저장 후 관련 부모를 잠그고 실제 활성 하위 행을 기준으로 조상을 아래에서 위로 재계산합니다.

### 검증
- 빠른 연속 완료 및 역순 응답을 모사한 상태 전파 검증을 추가했습니다.
- 새 SQL 함수와 `schema.sql`의 정의 일치를 확인했습니다.
