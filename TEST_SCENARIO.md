# Test Scenario

## 0. 테스트 전 준비

1. GitHub Pages 캐시 제거
2. 브라우저 개발자도구 Console 열기
3. Supabase `schema.sql` 최신 적용 여부 확인
4. 테스트 중 빨간 콘솔 에러가 없어야 함

## 1. 공통 로딩 테스트

대상:

- `index.html`
- `core.html`
- `raid.html`
- `overview.html`
- `party_generation.html`
- `parties.html`

성공 기준:

- 상단 메뉴 정상
- CSS 깨짐 없음
- 화면 하단 코드 노출 없음
- `</html>` 뒤 코드 없음
- 콘솔 에러 없음

## 2. index.html

### 계정/부계정

- 본계정 추가
- 부계정 추가
- 부계정이 본계정 하위 탭으로 표시되는지 확인
- 부계정 표시용 `...` 텍스트가 나오지 않는지 확인

### 캐릭터

- 캐릭터 추가/수정/삭제
- 아이콘 선택/제거
- 새로고침 후 유지

### 원정대 숙제

- 추가/수정/삭제
- 아이콘 선택/제거
- 체크 상태 저장
- 복제 기능 확인

### 일일/주간 숙제

- 추가/수정/삭제
- 수정 버튼 클릭 시 `setTaskIcon is not defined` 에러 없어야 함
- 아이콘 선택 위치 정상
- 체크 상태 저장
- 월간 숙제는 매월 첫째 주 수요일 06:00 KST 직전까지 완료 상태가 유지되고, 해당 시각부터 초기화되는지 확인
- 월간 휴식 게이지는 첫째 주 수요일 06:00 KST 경계를 한 번만 처리하는지 확인
- 일시중지 모드에서 캐릭터/주간·월간/계정 숙제를 중지하면 일반 비활성 숙제 보기에도 나타나지 않는지 확인
- 일시중지 관리에서 다시 활성화하면 기존 데이터가 보이고, 중지 기간의 휴식 게이지가 소급 충전되지 않는지 확인
- 상위 숙제 일시중지 시 하위 숙제도 숨겨지고, 다시 활성화 시 하위 완료 상태가 정상 동기화되는지 확인
- 일시중지 RPC를 고의로 실패시켰을 때 관련 숙제 중 일부만 DB에 반영되지 않는지 확인
- 네 상단 탭 모두에서 캐릭 업데이트 버튼이 보이는지 확인
- 일회용 숙제에서 캐릭터 숨기기/숨긴 캐릭터 다시 보기 및 새로고침 유지 확인

### 레이드 숙제

- 유통골드/귀속골드/더보기 골드 분리 표시
- 각 골드 hover 효과 확인
- 각 골드 토글 후 새로고침 유지
- 레이드 숙제 수정 버튼이 보이지 않아야 함
- 파티연동 hover 효과 확인
- 파티연동 클릭 시 해당 캐릭터가 속한 파티만 표시
- 파티 이동 버튼 클릭 시 `raid.html` 해당 파티로 이동

### 재화

- 금액 수정
- 아이콘 선택
- 최종 수정 날짜가 표시되지 않아야 함

## 3. core.html

- 상단 보기 탭 디자인 확인
- 편집 가능 버튼 스타일 확인
- 마우스 휠 스크롤 확인
- 기본 보기에서 코어 등급 변경
- 번호별 보기에서 접기/펼치기
- 사용자 조합 카드 크기 확인
- 등급 배지 hover 효과 확인
- 혼돈해1/혼돈달1/혼돈별1 클릭 편집
- 새로고침 후 혼돈 이름 유지
- 숨긴 코어 관리 확인

## 4. raid.html

### 파티 구성

- 레이드 추가
- 난이도 추가
- 파티 추가
- 파티 클릭 시 `gPresets is not defined` 에러 없음
- 여러 레이드/난이도/파티를 모두 클릭
- 파티 멤버 배치
- 8인 파티가 1파티 4명 / 2파티 4명으로 나뉘어 보이는지 확인
- 새로고침 후 유지

### 주간 일정

- 일정 추가/수정/삭제
- 일정 카드 우상단 즉시 삭제 버튼 확인
- 일정 카드의 멤버 `×` 임시 제거와 빈 슬롯 `+` 임시 추가 확인
- 즉시 일정 삭제 시 임시 추가/해제된 레이드 숙제 연동이 원상 복구되는지 확인
- 일정 삭제 RPC를 고의로 실패시켰을 때 일정과 레이드 숙제 연동이 모두 이전 상태로 유지되는지 확인
- 8인 일정 카드가 1파티 4명 / 2파티 4명으로 나뉘어 보이는지 확인
- 공지 입력
- 공지가 편집모드에서 잘 보여야 함
- 일정 완료/완료 해제
- index.html 및 overview.html과 완료 상태 연동

### 임시 파티

- 임시 추가
- 임시 삭제
- 원본 초기화
- 주차 이동
- 다음 주 복원

### 공유링크

- 공유 링크 생성
- 7/30/90일 유효기간으로 각각 생성하고 DB `expires_at`이 맞는지 확인
- 현재 링크 철회 후 같은 URL 접속이 `철회된 공유 링크`로 거절되는지 확인
- 만료 시각을 지난 링크가 `만료된 공유 링크`로 거절되는지 확인
- 시크릿 창에서 접속
- 공유 뷰어 진입 전후 `raid_schedule_overrides`, `raid_schedules`, `raid_tasks`가 변경되지 않는지 확인
- 뷰어 모드 배너 확인
- 주간 일정 확인
- 레이드 현황 탭 확인
- 캐릭터 필터 확인
- 완료 체크는 클릭되어도 수정되지 않아야 함
- 공지가 뷰어모드에서도 잘 보여야 함

## 5. overview.html

- 최초 진입 및 새로고침 후 캐릭터 필터가 접혀 있는지 확인
- 본계정/부계정 그룹화 확인
- 부계정 캐릭터들이 하나의 부계정 그룹 안에 카드로 표시되는지 확인
- 파티연동 클릭 시 해당 파티만 표시
- 완료 상태 연동 확인
- 파티X 및 임시해제 레이드 행 전체가 붉은 계열로 강조되는지 확인

## 6. party_generation.html

- 최초 진입 및 새로고침 후 캐릭터 필터가 접혀 있는지 확인
- 상단 우측 버튼 순서가 캐릭 업데이트 / 불러오기 / 원래대로 / 초기화 / 업로드 / 검증 / 적용인지 확인
- 한 레이드의 모든 난이도를 숨겨도 레이드 탭이 유지되고, 해당 탭에서 난이도를 다시 표시할 수 있는지 확인
- 숨김/후보 선택 작업안이 DB에 저장되고 새로고침 후 유지되는지 확인

## 7. parties.html

- 레이드명 옆 난이도 표시
- 난이도 색상 유지
- 아래 정보 줄에서 난이도 중복 제거
- 파티 멤버 슬롯 순서 확인
- 빈 슬롯 표시 확인
- 같은 파티의 같은 요일 일정이 여러 개여도 요일이 한 번만 표시되는지 확인
- 이번 주 고정 일정 override 이동 후 변경된 요일이 표시되는지 확인
- 지난 주 비고정 일정 요일이 이번 주에 표시되지 않는지 확인

## 8. DB 무결성

- 캐릭터 삭제 시 해당 `owner_type='character'` 일회용 숙제가 함께 삭제되는지 확인
- 계정 삭제 시 해당 계정과 하위 캐릭터의 일회용 숙제가 모두 삭제되는지 확인
- 마이그레이션 적용 시 이미 존재하던 orphan 일회용 숙제가 정리되는지 확인

## 9. 최종 합격 기준

```txt
[ ] 모든 페이지 콘솔 에러 없음
[ ] index.html 코드 노출 없음
[ ] core.html 마우스 휠 스크롤 정상
[ ] raid.html 파티 클릭 오류 없음
[ ] party_generation.html 필터 기본 접힘 및 숨김 레이드 탭 유지
[ ] 공유링크 레이드 현황 필터 정상
[ ] parties.html 난이도 위치 정상
[ ] 새로고침 후 데이터 유지
[ ] 다른 브라우저/시크릿 창에서도 정상
```

## 10. 원자성 및 주차 경계 회귀 테스트

- 하위 숙제가 2단계 이상인 루트 숙제를 여러 캐릭터/계정에 복제하고 모든 부모 관계가 동일한지 확인
- 같은 복제 그룹을 이미 가진 대상에 다시 복제했을 때 중복 루트가 생기지 않는지 확인
- 하위 숙제 완료로 상위 숙제가 자동 완료되는 순간 RPC를 실패시켜 둘 다 이전 상태로 롤백되는지 확인
- 마지막 두 개 이상의 하위 숙제를 빠르게 연속 완료하고 응답 순서를 뒤집어도 상위 숙제가 완료 상태로 남는지 확인
- 2단계 이상 중첩된 하위 숙제를 모두 완료하거나 하나를 해제했을 때 루트까지 완료/미완료가 전파되는지 확인
- 화요일 05:59:59, 06:00:00 KST에서 일일 완료와 휴식 게이지 경계가 정확히 갈리는지 확인
- 수요일 05:59:59, 06:00:00 KST에서 레이드 완료/주차/요일 표시가 모든 화면에서 동일한지 확인
- 이번 주 A 캐릭터를 임시해제하고 다음 주 다른 파티에 임시 추가해도 두 주의 표시가 서로 바뀌지 않는지 확인
- 임시해제 → 다른 파티 임시추가 → 제거를 반복한 뒤 원래 `raid_tasks.preset_id`가 유지되는지 확인
- 임시 파티 멤버를 정규 파티로 옮긴 뒤 해당 레이드 숙제가 정규 연결로 남는지 확인
- 주차가 바뀐 뒤 index/overview/parties/party_generation 중 어느 편집 화면을 먼저 열어도 지난 주 연결이 즉시 복원되는지 확인
- 공유 링크만 열었을 때 롤오버 RPC와 레이드 그룹 localStorage 마이그레이션 DB 쓰기가 발생하지 않는지 확인
- 1,001개 이상의 숙제/레이드/파티 행에서 마지막 페이지까지 누락 없이 같은 순서로 로드되는지 확인
